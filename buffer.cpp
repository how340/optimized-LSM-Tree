#include "buffer.h"

bool Buffer::insert(KEY_t key, VALUE_t val){

    // if buffer is full:
    //      return false. 
    // else: 
    //      if entry exists: 
    //          update value. 
    //      else: 
    //          insert new. 
    return 0;
}

VALUE_t* Buffer::get(KEY_t key){

    return 0;
}

KEY_t* Buffer::get_range(void){
    return 0
}

void Buffer::clear_buffer(void){

}